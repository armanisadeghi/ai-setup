# =============================================================================
# .env_secrets template — Copy to $WORKSPACE/.env_secrets and fill in values
#
# This file is gitignored and should NEVER be committed.
# =============================================================================

# AWS S3 (for model/output persistence)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_MODELS=matrx-models
AWS_REGION=us-east-2

# HuggingFace (for downloading models)
HF_TOKEN=

# CivitAI (for downloading models)
CIVITAI_API_KEY=

# GitHub PAT (for private repo access)
GIT_PAT=
